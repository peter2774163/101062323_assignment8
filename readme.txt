大製作法與助教給的socketsample相同,不過不需要第二個頁面的submit即可直接跳到calculator的部分,而在calculator部分與之前lab相同,除了最基本的加減乘除之外,我多加了x的y次方和x mod y的功能,而需要注意的為,在做x的y次方的功能實需要將變數做一些行轉換,及將原本的float轉成int. 而server的部分,因為其實我還是不太理解如何用android studio去執行server,因此我還是使用了eclipse新建了一個server的project並執行以達到一樣的效果. 而server裡面時做的部分就是在執行時先new出一個JFrame,並在server每次收到client所傳的數值時寫到JFrame上,大製作法與sample code相差無幾